\appendices
\section{Reproducibility Notes}

The final method config is \finalconfig{}. The frozen method keeps the backbone frozen and trains only \texttt{rgssb.*} and \texttt{box\_head.*}. NFS results must use the corrected normalized aligned XYWH annotation root. The invalid historical NFS rows are archived for provenance only and must not be used for current paper claims. FLOPs/MACs are unavailable and should not be reported as measured.
